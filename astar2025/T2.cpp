// Contest: Baidu Astar
//
// Powered by CPH-NG and VSCode

#include <bits/stdc++.h>
#define int long long
using namespace std;
const int N = 4e5 + 10;

int n, m, K, l;
int x[N];
int L[N];

struct node {
    int c, p, q;
    vector<int> in, out;
};

vector<int> adj[N];

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    return 0;
}
